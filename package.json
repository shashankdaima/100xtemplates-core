{
  "name": "100xtemplates-core",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\" && uv run black . && uv run isort .",
    "check-types": "turbo run check-types",
    "py:init": "uv venv && uv pip install -e .",
    "py:dev": "cd apps/fastapi-app && uv run uvicorn main:app --reload --host 0.0.0.0 --port 5000",
    "py:build": "uv pip install -e .",
    "py:test": "uv run pytest",
    "py:lint": "uv run black . && uv run isort . && uv run mypy . && uv run ruff .",
    "py:format": "uv run black . && uv run isort .",
    "py:check": "uv run ruff .",
    "py:clean": "rm -rf .venv",
    "tree:generate": "tree -I 'node_modules|.venv|.turbo' > tree.txt"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.4.4",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages-py/*"
  ]
}
